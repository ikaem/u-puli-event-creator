export type EventModel = {
	id: number;
	title: string;
	location: string;
	url: string;
	imageUrl: string;
	description: string;
	date: string; // date in format DD.MM.YYYY.
	time: string; // time in format HH:mm
};
